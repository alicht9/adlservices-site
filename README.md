# adlservices.pro

Static landing page for ADL Services, Flagstaff AZ. One file, no build step.

Served by GitHub Pages at https://adlservices.pro — the `CNAME` file is what
binds the custom domain, so don't delete it.

Edit `index.html`, push to `main`, and it's live in about a minute.
